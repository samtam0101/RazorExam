@page
@model RazorApp.Pages.Transaction.CreateTransactionModel
@{
    ViewData["Title"] = "Create New Transaction";
}

<!DOCTYPE html>
<html>
<head>
    <title style="text-align: center">@ViewData["Title"]</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        /* Стили для страницы создания новой категории */
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            color: #333;
            padding: 20px;
        }

        .form-container {
            max-width: 500px;
            margin: 0 auto;
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 5px;
            padding: 30px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .btn-cancel {
            margin-right: 10px;
        }
    </style>
</head>
<body>
<div class="container">
    <h1>@ViewData["Title"]</h1>

    <div class="form-container">
        <form method="post">
            <div class="form-group">
                <label asp-for="CreateTransactionDto.FromAccountId"> From Account Id:</label>
                <input asp-for="CreateTransactionDto.FromAccountId" class="form-control"/>
                <span asp-validation-for="CreateTransactionDto.FromAccountId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CreateTransactionDto.ToAccountId">To Account Id:</label>
                <input asp-for="CreateTransactionDto.ToAccountId" class="form-control"/>
                <span asp-validation-for="CreateTransactionDto.ToAccountId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CreateTransactionDto.Amount">Amount:</label>
                <input asp-for="CreateTransactionDto.Amount" class="form-control"/>
                <span asp-validation-for="CreateTransactionDto.Amount" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CreateTransactionDto.TransactionDate">Transaction Date:</label>
                <input asp-for="CreateTransactionDto.TransactionDate" class="form-control"/>
                <span asp-validation-for="CreateTransactionDto.TransactionDate" class="text-danger"></span>
            </div>


            <!-- Добавьте другие поля DTO здесь, если они есть -->

            <button type="submit" class="btn btn-primary">Create Transaction</button>
            <a asp-page="/Transaction/GetTransactions" class="btn btn-secondary btn-cancel">Cancel</a>
        </form>
    </div>
</div>

<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>