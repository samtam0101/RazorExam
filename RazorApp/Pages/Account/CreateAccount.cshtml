@page
@model RazorApp.Pages.Account.CreateAccountModel
@{
    ViewData["Title"] = "Create New Account";
}

<!DOCTYPE html>
<html>
<head>
    <title style="text-align: center">@ViewData["Title"]</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        /* Стили для страницы создания новой категории */
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            color: #333;
            padding: 20px;
        }

        .form-container {
            max-width: 500px;
            margin: 0 auto;
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 5px;
            padding: 30px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .btn-cancel {
            margin-right: 10px;
        }
    </style>
</head>
<body>
<div class="container">
    <h1>@ViewData["Title"]</h1>

    <div class="form-container">
        <form method="post">
            <div class="form-group">
                <label asp-for="CreateAccountDto.AccountNumber"> Account Number:</label>
                <input asp-for="CreateAccountDto.AccountNumber" class="form-control"/>
                <span asp-validation-for="CreateAccountDto.AccountNumber" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CreateAccountDto.Balance">Balance:</label>
                <input asp-for="CreateAccountDto.Balance" class="form-control"/>
                <span asp-validation-for="CreateAccountDto.Balance" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CreateAccountDto.OwnerId">Owner Id:</label>
                <input asp-for="CreateAccountDto.OwnerId" class="form-control"/>
                <span asp-validation-for="CreateAccountDto.OwnerId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CreateAccountDto.Type">Type:</label>
                <input asp-for="CreateAccountDto.Type" class="form-control"/>
                <span asp-validation-for="CreateAccountDto.Type" class="text-danger"></span>
            </div>


            <!-- Добавьте другие поля DTO здесь, если они есть -->

            <button type="submit" class="btn btn-primary">Create Account</button>
            <a asp-page="/Account/GetAccounts" class="btn btn-secondary btn-cancel">Cancel</a>
        </form>
    </div>
</div>

<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>